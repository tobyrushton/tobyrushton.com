---
interface Props {
    title: string
}

const { title } = Astro.props
---
<html lang="en-gb">
	<head>
		<meta charset="utf-8" />
		<link rel="icon" type="image/svg+xml" href="/favicon.png" />
		<meta name="viewport" content="width=device-width" />
		<meta name="generator" content={Astro.generator} />
		<title>{title}</title>
	</head>
	<body class="bg-bg flex flex-col min-h-screen text-white w-screen">
		<slot />
		<footer>
            <div class="p-6 text-text-secondary text-xs">
                <p>
                    © {new Date().getFullYear()} Toby Rushton
                </p>
            </div>
        </footer>
	</body>
</html>